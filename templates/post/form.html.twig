{% extends "base.html.twig" %}

{% block title %}
    Gestion Administrative | Le Quai Antique
{% endblock %}

{% block body %}
	<div class="container-fluid">
        <h1 class="main-title-admin-page">Gestion Administrative</h1>
		<h2>Plats présentés en page de garde</h2>
		{{ form_start(post_form, {"attr": {"novalidate": "novalidate"}}) }}
		{{ form_row(post_form.title) }}
		{{ form_row(post_form.content) }}
		{{ form_row(post_form.image) }}
		<button type="submit" class="btn btn-primary mt-3">Créer</button>
		{{ form_end(post_form)}}
	</div>
{% endblock %}
