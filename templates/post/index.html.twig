{% extends 'base.html.twig' %}

{% block title %}Accueil | Le Quai Antique
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-around flex-wrap">
		{% for post in posts %}
			<div class="card mt-5" style="width: 18rem;">
				{% if post.title %}
					{# Mettre le titre en apparence avec passage de la souris sur une photo #}
					<h5 class="card-title">{{ post.title }}</h5>
				{% endif %}
				{% if post.image %}
					<img src={{ post.image }} class="card-img-top" alt={{ post.title }}>
				{% endif %}
				<div class="card-body">
					<p class="card-text">{{ post.content }}</p>
					{% if is_granted('ROLE_ADMIN') %}
						<a href="{{ path('delete-post', {'id': post.id}) }}" class="btn btn-danger">Supprimer</a>
						<a href="{{ path('edit-post', {'id': post.id}) }}" class="btn btn-warning">Modifier</a>
						<a href="{{ path('copy-post', {'id': post.id}) }}" class="btn btn-info">Copier</a>
					{% endif %}
				</div>
			</div>
		{% endfor %}
	</div>
	<div class="d-flex justify-content-center flex-wrap mt-5">
		<a href="#" class="btn btn-danger">Réserver</a>
	</div>

	<div style="height: 800px;">
	</div>

	<!--Footer Section-->

	<footer class="container-fluid footer mt-5">
		<div class="row">
			<div class="col-sm-3 mx-auto text-center footer-main-article">
				<h5 class="footer-main-title">MPC-AGENCY</h5>
				<p class="mt-3">Entreprise présente dans les alpes-maritimes</p>
				<p>Nous vous livrons de Nice à Mandelieu</p>
			</div>
			<div class="col-sm-3 mx-auto text-center footer-main-article">
				<h5 class="footer-title">Contact</h5>
				<a href="tel:+33749991449" style="color: white;text-decoration: none;">Anthony,<br>07.49.99.14.49<br></a>
				<a href="tel:+33698062012" style="color: white;text-decoration: none;">Maxime Coquerelle,<br>06.98.06.20.12</a>
			</div>
			<div class="col-sm-3 mx-auto text-center footer-article">
				<h5 class="footer-title">Mentions Légales</h5>
				<a href="#" style="color: white;text-decoration: none;">
					<i class="bi bi-c-circle"></i>
					MPC-Agency 2023</a>
			</div>
			<div class="col-sm-3 mx-auto text-center footer-article">
				<h5 class="footer-title">Liens</h5>
				<a href="#" style="color: white;text-decoration: none;padding-right: 1rem;">
					<i class="bi bi-facebook"></i>
				</a>
				<a href="#" style="color: white;text-decoration: none;padding-right: 1rem;">
					<i class="bi bi-twitter"></i>
				</a>
				<a href="#" style="color: white;text-decoration: none;">
					<i class="bi bi-instagram"></i>
				</a>
			</div>
		</div>
	</footer>
{% endblock %}
